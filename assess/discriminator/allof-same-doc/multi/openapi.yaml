---
openapi: "3.0.3"
info:
  version: "0.0.1"
  title: "allOf discriminator with same-document components (Entry File)"
paths:
  /foo:
    get:
      responses:
        "200":
          description: Ok
          content:
            application/json:
              schema:
                $ref: components.yaml#/components/schemas/foo
